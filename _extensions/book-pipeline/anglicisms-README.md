# Anglicisms Filter (anglicisms.lua)

Фильтр для поиска английских слов (англицизмов) в русском тексте.

## Назначение

- 🔍 Находит английские слова в русском тексте
- ✅ Проверяет whitelist технических терминов
- 💡 Предлагает русские альтернативы
- 📄 Генерирует подробный отчет

## Использование

### 1. Добавьте в _quarto.yml

```yaml
filters:
  - anglicisms.lua
```

### 2. Соберите документ

```bash
quarto render your-document.qmd
```

### 3. Проверьте отчет

После сборки будет создан файл `_anglicisms_report.md` с результатами.

## Пример

**Входной текст:**
```markdown
# DevRel менеджер

DevRel manager проводит meeting каждую неделю.
Используем Docker и API для deployment проекта.
```

**Результат в отчете:**
```markdown
## Найденные англицизмы

- **deployment** → деплой / развертывание
- **manager** → менеджер
- **meeting** → встреча / митинг

## Разрешенные термины (whitelist)

- API
- DevRel
- Docker
```

## Whitelist

Фильтр содержит обширный whitelist технических терминов:

### Технологии
- Docker, Kubernetes, API, SDK, CLI, GUI
- JavaScript, TypeScript, Python, Java, React, Vue
- HTML, CSS, JSON, YAML, Markdown, PDF

### Платформы
- GitHub, GitLab, Slack, Discord, Telegram
- AWS, Azure, GCP

### Инструменты
- Git, npm, yarn, Jira, Confluence

**Полный список:** см. переменную `whitelist` в `anglicisms.lua`

## Русские альтернативы

Фильтр предлагает русские альтернативы для частых слов:

| Английское | Русское |
|------------|---------|
| manager | менеджер |
| meeting | встреча / митинг |
| feedback | обратная связь / фидбек |
| team | команда |
| deadline | дедлайн / срок |
| feature | функция / фича |
| bug | баг / ошибка |
| deploy | деплой / развертывание |

**Полный список:** см. словарь `replacements` в `anglicisms.lua`

## Настройка

### Добавить слово в whitelist

Отредактируйте `anglicisms.lua`:

```lua
local whitelist = {
  -- ... существующие термины ...
  "YourTerm",  -- Добавьте свой термин
}
```

### Добавить альтернативу

Отредактируйте словарь `replacements`:

```lua
local replacements = {
  -- ... существующие ...
  ["yourword"] = "ваше_слово",
}
```

## Формат отчета

Отчет `_anglicisms_report.md` содержит:

1. **Найденные англицизмы** - список с предложениями замен
2. **Разрешенные термины** - технические термины из whitelist
3. **Рекомендации** - что делать с найденными словами

## Рекомендации по использованию

### ✅ Хорошо

- Используйте технические термины на английском там, где это принято (API, Docker)
- Заменяйте общие слова на русские (manager → менеджер)
- Будьте последовательны в терминологии

### ⚠️ Осторожно

- Не заменяйте все подряд - иногда английский термин точнее
- Учитывайте аудиторию - для технических читателей больше англицизмов OK
- Проверяйте контекст - "release" может быть "релиз" или "выпуск"

## Примеры команд

```bash
# Собрать документ с проверкой англицизмов
quarto render book.qmd

# Посмотреть отчет
cat _anglicisms_report.md

# Добавить в .gitignore если не нужен в репозитории
echo "_anglicisms_report.md" >> .gitignore
```

## Интеграция с CI

Можно автоматизировать проверку:

```yaml
# .github/workflows/check-anglicisms.yml
name: Check Anglicisms

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Install Quarto
        uses: quarto-dev/quarto-actions/setup@v2
      
      - name: Render and check
        run: |
          quarto render book.qmd
          if [ -f _anglicisms_report.md ]; then
            cat _anglicisms_report.md
          fi
```

## Технические детали

### Алгоритм

1. **Сканирование** - проходит все текстовые элементы (`Str`)
2. **Извлечение** - находит последовательности латинских букв
3. **Проверка** - определяет, является ли слово английским
4. **Фильтрация** - проверяет whitelist
5. **Накопление** - собирает данные для отчета
6. **Генерация** - создает отчет в конце (`Pandoc`)

### Эвристики определения английских слов

Слово считается английским если:
- Содержит только латинские буквы
- Длина ≥ 3 символа
- Имеет типичные английские суффиксы (-ing, -tion, -ment, -ed, -er, -ly)
- ИЛИ есть в словаре `replacements`

### Производительность

- ✅ Быстрая обработка (не влияет на время сборки)
- ✅ Работает с документами любого размера
- ✅ Минимальное использование памяти

## Лицензия

Часть Book Pipeline проекта.

---

**Автор:** Book Pipeline Team  
**Версия:** 1.0  
**Дата:** Октябрь 2025

