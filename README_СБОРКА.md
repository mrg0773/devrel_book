# 📚 Сборка книги "База DevRel"

## 🚀 Быстрый старт

**ЕДИНСТВЕННЫЙ ПРАВИЛЬНЫЙ СПОСОБ сборки:**

```bash
cd "/Users/rafaelminiakhmetov/second_mind/Книги/Книги/База деврела"
bash scripts/build-with-version.sh
```

**ВАЖНО:** НЕ запускайте `quarto render` напрямую! Используйте только `build-with-version.sh`.

**Скрипт автоматически:**
- ✅ Увеличивает версию (patch: 1.0.27 → 1.0.28)
- ✅ Синхронизирует глоссарий (YAML → JSON)
- ✅ Заменяет термины во временных файлах
- ✅ Проверяет качество контента
- ✅ Собирает PDF
- ✅ Создает Git коммит и тег
- ✅ Архивирует в `_versions/`
- ✅ Открывает готовый PDF

**Не нужно ничего настраивать или запускать вручную.**

---

## 📋 Результаты сборки

После выполнения `./build-with-version.sh` вы получите:

- **PDF**: `_output/База-DevRel-v{VERSION}.pdf` (~1.2 MB)
- **HTML**: встроенная книга с навигацией и поиском
- **Логи**: `_output/build-v{VERSION}.log`
- **Архив**: `_versions/v{VERSION}/`
- **Git тег**: `book-v{VERSION}`

---

## 🔧 Требования

✅ **Quarto 1.8.25+** - установлен  
✅ **Pandoc 3.8.2+** - установлен  
✅ **TinyTeX v2025.10** - установлен  
✅ **Python 3.x** - для pre-render hooks

---

## 🏗️ Как это работает (под капотом)

### Автоматический workflow:

```
1. ./build-with-version.sh
   ↓
2. Bump версии (bump-version.sh)
   → Обновляет _quarto.yml и index.qmd
   ↓
3. Проверки качества
   → check-quality.py (мастер-словарь)
   → fix-wrong-terms.py (Б2Б→B2B, СаАС→SaaS)
   ↓
4. Pre-render: sync_glossary.py
   → glossary/terms.yml → СЛОВАРЬ_ТЕРМИНОВ.json
   ↓
5. Pre-render: apply_terms_prerender.py
   → Читает исходники из Черновики глав/
   → Заменяет <TERM:...>> на значения из словаря
   → Создает _temp_build/ с замененными терминами
   → Исходники остаются с плейсхолдерами!
   ↓
6. Quarto render
   → Собирает книгу из _temp_build/
   → Применяет Lua-фильтры (истории, emoji)
   → Генерирует PDF
   ↓
7. Post-render: cleanup_temp.py
   → Удаляет _temp_build/
   ↓
8. Git commit + tag
   → Коммит с версией
   → Тег book-v{VERSION}
   ↓
9. Архивация
   → Копия в _versions/v{VERSION}/
   ↓
10. Готово! PDF открывается автоматически
```

### Ключевые особенности:

**Неизменность исходников:**
- Исходники в `Черновики глав/` содержат плейсхолдеры `<TERM:devrel>>`
- При сборке плейсхолдеры заменяются автоматически во временных файлах
- После сборки временные файлы удаляются
- Git фиксирует только исходники с плейсхолдерами

**Единый источник терминов:**
- `glossary/terms.yml` - единственное место для редактирования терминов
- Автоматическая конвертация в JSON перед каждой сборкой
- Умная замена с учетом контекста (заглавные буквы в заголовках)

---

## 📝 Редактирование книги

### Редактирование контента глав:

1. Откройте файл главы в `Черновики глав/`
2. Редактируйте как обычный Markdown
3. Для терминов используйте плейсхолдеры: `<TERM:devrel>>`
4. Запустите `./build-with-version.sh`
5. Термины автоматически заменятся при сборке

### Редактирование терминов:

1. Откройте `glossary/terms.yml`
2. Найдите нужный термин:
```yaml
- term: devrel
  value: деврел
  cases:
    родительный: деврела
    дательный: деврелу
    винительный: деврел
```
3. Измените значение
4. Запустите `./build-with-version.sh`
5. Все вхождения термина обновятся автоматически

### Добавление нового термина:

**Шаг 1:** Добавьте в `glossary/terms.yml`:
```yaml
- term: новый_термин
  value: Новое Значение
  cases:
    родительный: нового значения
    множественное: новые значения
```

**Шаг 2:** Используйте в главах:
```markdown
## Что такое <TERM:новый_термин>>

Описание <TERM:новый_термин_родительный>>.
```

**Шаг 3:** Соберите книгу:
```bash
./build-with-version.sh
```

---

## 🔄 Управление версиями

### Автоматическое версионирование (по умолчанию):

```bash
./build-with-version.sh
# Автоматически: 1.0.27 → 1.0.28 (patch)
```

### Ручное версионирование:

**Minor версия (1.0.27 → 1.1.0):**
```bash
./bump-version.sh minor
./build-with-version.sh
```

**Major версия (1.0.27 → 2.0.0):**
```bash
./bump-version.sh major
./build-with-version.sh
```

**Отключить автобамп:**
```bash
AUTO_BUMP=false ./build-with-version.sh
```

### Где хранится версия:

- `_quarto.yml` - строка `version: "1.0.27"`
- `index.qmd` - в метаданных книги
- Git теги - `book-v1.0.27`

---

## 📂 Структура проекта

```
База деврела/
├── _quarto.yml                    # Конфигурация Quarto + pre/post-render hooks
├── index.qmd                      # Главная страница книги
│
├── build-with-version.sh          # 🔨 ГЛАВНЫЙ СКРИПТ (запускайте его!)
├── bump-version.sh                # Увеличение версии (вызывается автоматически)
│
├── sync_glossary.py               # Pre-render: YAML → JSON
├── apply_terms_prerender.py       # Pre-render: создание _temp_build/
├── cleanup_temp.py                # Post-render: удаление _temp_build/
│
├── glossary/
│   └── terms.yml                  # 📖 ЕДИНЫЙ ИСТОЧНИК ТЕРМИНОВ (редактируйте тут!)
│
├── СЛОВАРЬ_ТЕРМИНОВ.json          # Автогенерируется из terms.yml
│
├── Черновики глав/                # Исходники с плейсхолдерами <TERM:...>>
│   ├── Введение.md
│   ├── Глава_01_Что_такое_DevRel.md
│   ├── ...
│   └── Приложение_Г_QA.md
│
├── _temp_build/                   # Временные файлы (создается при сборке)
│                                  # Исходники с замененными терминами
│                                  # Автоматически удаляется после сборки
│
├── _output/                       # Результаты сборки
│   ├── База-DevRel-v1.0.27.pdf
│   └── build-v1.0.27.log
│
└── _versions/                     # Архив всех версий
    ├── v1.0.25/
    ├── v1.0.26/
    └── v1.0.27/
```

---

## 🛠️ Автоматические проверки

При каждой сборке выполняются проверки:

1. **Плейсхолдеры** - подсчет `{{АНАЛОГИЯ`, `{{ИСТОРИЯ`, `{{ШУТКА`
2. **Ручная нумерация** - проверка отсутствия "## 4.1" в заголовках
3. **Заглавные буквы** - "деврел" → "Деврел" в начале заголовков
4. **Качество контента** - мастер-словарь (`check-quality.py`)
5. **Терминология** - проверка на Б2Б→B2B, СаАС→SaaS (`fix-wrong-terms.py`)
6. **Unicode символы** - автозамена через `fix-emoji.lua`

**Если проверка не прошла** - сборка остановится с сообщением об ошибке.

---

## 🎨 Pre-render и Post-render Hooks

Настроено в `_quarto.yml`:

```yaml
project:
  pre-render: 
    - python3 sync_glossary.py           # Конвертация глоссария
    - python3 apply_terms_prerender.py   # Замена терминов
  post-render:
    - python3 cleanup_temp.py            # Очистка временных файлов
```

### Pre-render hooks:

**1. sync_glossary.py**
- Читает `glossary/terms.yml`
- Конвертирует в `СЛОВАРЬ_ТЕРМИНОВ.json`
- Создает все падежи терминов

**2. apply_terms_prerender.py**
- Читает исходники из `Черновики глав/`
- Заменяет `<TERM:ключ>>` на значения из словаря
- Создает `_temp_build/` с замененными терминами
- Умная логика: заглавные буквы в заголовках

### Post-render hook:

**cleanup_temp.py**
- Удаляет директорию `_temp_build/`
- Оставляет исходники чистыми

---

## 🔍 Lua-фильтры

Применяются во время сборки (порядок важен):

1. `replace-terms.lua` - ⚠️ DEPRECATED (замена через Python)
2. `replace-placeholders.lua` - замена историй/аналогий `{{АНАЛОГИЯ:...}}`
3. `anglicisms.lua` - проверка англицизмов
4. `fix-emoji.lua` - очистка emoji для PDF

**Примечание:** Замена терминов теперь происходит на уровне Python (pre-render), а не через Lua-фильтр.

---

## 📖 Форматы плейсхолдеров

### Термины (автоматическая замена):

```markdown
<TERM:devrel>>           → деврел
<TERM:devrel_родительный>> → деврела
<TERM:saas>>             → SaaS
```

### Истории и аналогии (Lua-фильтры):

```markdown
{{АНАЛОГИЯ:название_файла}}
{{ИСТОРИЯ:название_файла}}
{{ШУТКА:название_файла}}
```

---

## 🐛 Troubleshooting

### Проблема: Нет кириллицы в PDF

**Решение:** Убедитесь что в `_quarto.yml` указаны шрифты с поддержкой Unicode:
```yaml
mainfont: "PT Serif"
sansfont: "Fira Sans"
```

### Проблема: TinyTeX не установлен

**Решение:**
```bash
quarto install tinytex --update-path
```

### Проблема: Ошибка при сборке главы

**Решение:** Проверьте синтаксис Markdown в проблемной главе.

### Проблема: Термины не заменяются

**Проверьте:**
1. Формат плейсхолдера: `<TERM:ключ>>` (два `>` в конце!)
2. Термин существует в `glossary/terms.yml`
3. Pre-render hooks выполняются (смотрите вывод консоли)

### Проблема: Версия не увеличивается

**Решение:**
```bash
# Проверьте что скрипт исполняемый
chmod +x build-with-version.sh bump-version.sh

# Запустите вручную
./bump-version.sh patch
./build-with-version.sh
```

---

## 📚 Дополнительная документация

- **ФИНАЛЬНЫЙ_ОТЧЕТ_ПЛЕЙСХОЛДЕРЫ.md** - История миграции системы плейсхолдеров
- **ОТЧЕТ_LUA_ФИЛЬТРЫ.md** - Описание Lua-фильтров для историй/аналогий
- **✅_СИСТЕМА_УНИФИЦИРОВАНА_v2.0.md** - Документация единого глоссария
- **VERSION.md** - История версий книги
- **README_ТЕРМИНЫ.md** - Детали работы с терминами

---

## 📊 Метаданные книги

- **Название**: База DevRel
- **Подзаголовок**: Практическое руководство по Developer Relations и бренду работодателя
- **Авторы**: Рафаэль Миниахметов, Ксения Романова
- **Язык**: Русский (ru)
- **Формат**: A4, 11pt, scrbook
- **Текущая версия**: 1.0.27

---

## 📅 История версий

### v1.0.27 - 03.11.2025 (текущая)
- ✅ Система плейсхолдеров `<TERM:...>>` полностью работает
- ✅ Pre-render hooks для автоматической замены терминов
- ✅ Исходники остаются с плейсхолдерами
- ✅ Автоматическое версионирование

### v1.0.22 - 03.11.2025
- ✅ Миграция с `{{TERM:...}}` на `<TERM:...>>`
- ✅ Исправлен конфликт с Quarto bracketed spans
- ✅ Все плейсхолдеры терминов работают

### v1.0.5 - 02.11.2025
- ✅ Унификация системы глоссария
- ✅ glossary/terms.yml как единый источник
- ✅ Автоматическая синхронизация YAML → JSON

### v1.0.0 - 31.10.2025
- ✅ Первый стабильный релиз
- ✅ Все главы готовы
- ✅ Система проверок качества

---

## 🎯 Лучшие практики

### ✅ DO (делайте так):

1. **Редактируйте только `glossary/terms.yml`** для терминов
2. **Используйте `./build-with-version.sh`** для сборки
3. **Коммитьте исходники с плейсхолдерами** (не замененные термины)
4. **Проверяйте логи** в `_output/build-v{VERSION}.log`
5. **Используйте Git теги** для отката к версиям

### ❌ DON'T (не делайте так):

1. ❌ Не редактируйте `СЛОВАРЬ_ТЕРМИНОВ.json` вручную (автогенерируется!)
2. ❌ Не запускайте `quarto render` напрямую (используйте скрипт!)
3. ❌ Не изменяйте файлы в `_temp_build/` (временная директория!)
4. ❌ Не коммитьте файлы с замененными терминами
5. ❌ Не используйте старый формат `{{TERM:...}}` (устарел!)

---

## 🚀 Быстрые команды

```bash
# ПРАВИЛЬНО: Собрать новую версию книги (автобамп patch)
bash scripts/build-with-version.sh

# ПРАВИЛЬНО: Собрать без увеличения версии
AUTO_BUMP=false bash scripts/build-with-version.sh

# ❌ НЕПРАВИЛЬНО: НЕ запускайте напрямую!
# quarto render --to pdf  # НЕ ДЕЛАЙТЕ ТАК!

# Проверка системы
bash ПРОВЕРКА_РАБОТЫ.sh

# Проверка исходников на мусор
python3 scripts/check-garbage.py --dir "Черновики глав/" --strict

# Посмотреть текущую версию
grep "^version:" _quarto.yml | head -1

# Открыть последний PDF
open _output/База-DevRel-v*.pdf
```

---

**Дата создания:** 22 октября 2025  
**Последнее обновление:** 3 ноября 2025  
**Версия документа:** 2.0
